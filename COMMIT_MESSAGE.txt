Build Luna-UI-Rust M2 editor-grade text foundation

Add luna-text with stable line-plus-UTF-8 positions, explicit scalar-boundary snapping,
anchor/focus ranges, immutable line metadata, location/absolute conversions, Unicode extended
grapheme boundaries, compact editable state, selection replacement, grapheme-safe backward and
forward deletion, preferred-column vertical motion, edit revisions, and saturating scroll state.

Add luna-text-cosmic as the only crate that owns cosmic-text types. Retain one FontSystem and
SwashCache, use advanced shaping with installed-font fallback, capture bidi-aware grapheme caret
stops, rasterize glyphs into an immutable transparent BGRA8 snapshot, and expose deterministic
caret, hit-test, selection, exact visible-range, and measured horizontal/vertical scroll geometry
without retaining font-system borrows inside widgets.

Extend luna-render with immutable shared RasterImage storage, clipped image display commands,
nearest-neighbor DPI scaling, and correct straight-alpha source-over composition. Add regression
coverage proving transparent glyph intermediates retain straight color and disjoint clips draw
nothing.

Extend platform-neutral accessibility with a multiline text-area role, textual values, editability,
and complete, caret, selected, and visible UTF-8 ranges. Map the text-area role and value through the
AccessKit leaf adapter while preserving M1 stable IDs and corrected target_node action routing.

Add the reusable Luna TextView. Derive background, gutter, current-line highlight, selection,
glyph pixels, caret, pointer-to-text mapping, scrolling limits, caret reveal, visible semantic lines,
and text ranges from one immutable shaped snapshot.

Add the native luna-ui-rust-text-demo with multilingual fallback and bidi fixtures, ligatures,
combining graphemes, emoji sequences, keyboard-layout text plus IME commits, keyboard editing,
Control-A, click and
drag selection, mouse-wheel and Page Up/Down scrolling, resize/DPI behavior, accessibility focus,
and Escape-to-exit.

Update architecture, status, porting, roadmap, research, upgrade, validation, and README guidance for
M2 while retaining Rust 1.97.1, Edition 2024, resolver 3, MPL-2.0, strict rustdoc, forbidden unsafe
code, and warnings-as-errors policy.
