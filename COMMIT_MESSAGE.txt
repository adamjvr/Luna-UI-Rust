Build Luna-UI-Rust M1 native host and layout foundation

Advance the compiler-verified M0 core into the first live native desktop milestone without leaking
winit, softbuffer, or AccessKit types into reusable Luna contracts.

Add luna-layout with deterministic row, column, stack, and two-pane split calculations. Return
immutable geometry snapshots, distribute integer remainders deterministically, saturate fixed and
gap allocation in undersized containers, and cover ordering, alignment, small-window, and minimum
pane behavior with regression tests.

Add luna-commands with validated typed command IDs, command metadata, normalized key chords,
repeat policy, deterministic BTreeMap registration, and immutable command dispatch requests. Keep
behavior execution in applications rather than storing callbacks inside widgets.

Extend luna-input ordering traits for deterministic key-binding maps and extend accessibility nodes
with disabled and focused builder state.

Extend the safe CPU renderer with logical-to-physical fractional-DPI rectangle conversion while
retaining the one-to-one M0 path and BGRA8 framebuffer as the executable rendering oracle.

Add luna-accessibility-accesskit as a leaf adapter. Preserve stable numeric AccessKit IDs across
frames, translate roles, labels, bounds, children, disabled state, focus, and available actions, and
produce complete tree updates suitable for the event-loop-proxy adapter lifecycle.

Add luna-host-winit using winit 0.30 ApplicationHandler and run_app lifecycle, hidden-first window
creation for AccessKit, safe softbuffer context and surface management, DPI-aware framebuffer
allocation, resize and redraw handling, normalized pointer/keyboard/text/scroll/focus input,
AccessKit action routing, and explicit runtime error propagation.

Add a product-neutral WorkspaceDemo composite fixture built from shared layout snapshots. Use the
same calculated bounds for display-list generation, hit testing, and accessibility semantics.

Add the live luna-ui-rust-native-demo application with Control-P typed command dispatch, pointer
activation, semantic focus, AccessKit click/focus actions, resize/DPI behavior, and Escape-to-exit.
Keep the original headless PPM demo as the deterministic CI-friendly proof path.

Update architecture, status, porting, roadmap, research, validation, README, and CI-facing guidance
for M1 while retaining Rust 1.97.1, Edition 2024, resolver 3, MPL-2.0, strict rustdoc, forbidden
unsafe code, and warnings-as-errors policy.
