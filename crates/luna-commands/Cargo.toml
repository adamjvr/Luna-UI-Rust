[package]
name = "luna-commands"
description = "Typed command identifiers, key bindings, and dispatch requests for Luna UI Rust."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
luna-core = { path = "../luna-core" }
luna-input = { path = "../luna-input" }

[lints]
workspace = true
